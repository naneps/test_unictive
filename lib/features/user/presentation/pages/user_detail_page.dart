import 'package:flutter/material.dart';

class UserDetailPage extends StatelessWidget {
  int id;
  UserDetailPage({super.key, required this.id});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
